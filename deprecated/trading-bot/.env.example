# AI Trading Bot Platform - Environment Variables
# This file serves as documentation for all environment variables used in the platform.
# Copy this file to .env and fill in the appropriate values.

# ===== GENERAL =====
NODE_ENV=development                       # development, test, or production

# ===== PLATFORM STARTUP =====
PLATFORM_MODE=development                  # development or production
PLATFORM_LOG_LEVEL=info                    # debug, info, warn, error
FRONTEND_PORT=5173                         # Frontend service port
BACKEND_PORT=3000                          # Backend service port (or will find available in range 3000-3020)
BOT_PORT=5555                              # Bot microservice port
PLATFORM_CONFIG_PATH=                      # Optional path to custom platform config

# ===== BACKEND SERVER =====
PORT=3000                                  # Backend server port
COOKIE_SECRET=your-cookie-secret           # Secret for cookie signing
CORS_ORIGIN=*                              # CORS allowed origins (comma-separated for multiple)

# ===== JWT AUTHENTICATION =====
JWT_SECRET=your-jwt-secret-key             # Secret for JWT token signing
JWT_ACCESS_EXPIRATION=15m                  # Access token expiration time
JWT_REFRESH_EXPIRATION=7d                  # Refresh token expiration time

# ===== DATABASE =====
# Postgres credentials (used in test/production)
DB_USER=postgres                           # Database username
DB_PASSWORD=postgres                       # Database password
DB_NAME=trading_platform                   # Database name
DB_HOST=localhost                          # Database host
DB_PORT=5432                               # Database port

# ===== BOT MICROSERVICE =====
# Database configurations
DATA_NAME=data_table                       # Data table name
DB_PASSPHRASE=Passphrase                   # Database passphrase
DB_LOG_NAME=../databases/db_operations.log # Database log file name
DB_LOG_LEVEL=INFO                          # Database log level (INFO, DEBUG, ERROR)

# Debug configurations
DEBUG=1                                    # Enable debug mode (0/1)
USE_SECURE_CONNECTION=0                    # Use secure connection (0/1)

# Broker API Credentials
ALPACA_KEY=your-alpaca-key                 # Alpaca API key
ALPACA_SECRET=your-alpaca-secret           # Alpaca API secret

# Trading Configurations
TRADE_VALUE_PERCENT=0.1                    # Percentage of account to use per trade
TRADE_VALUE_CAP=1000                       # Maximum value per trade

# ===== FRONTEND =====
GEMINI_API_KEY=your-gemini-api-key         # Gemini API key for frontend services